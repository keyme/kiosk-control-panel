apiVersion: v1
kind: Service
metadata:
  name: {{ include "kiosk-control-panel.fullname" . }}
  labels:
    {{- include "kiosk-control-panel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kiosk-control-panel.selectorLabels" . | nindent 4 }}
